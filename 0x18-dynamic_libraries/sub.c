#include "main.h"

/**
 * sub - subtracts a number from another
 * @a: first number
 * @b: second number
 * Return: returns the value of the subtraction
 */

int sub(int a, int b)
{
return (a - b);
}
